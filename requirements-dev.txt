# Test and tooling only. Never installed into the runtime image: every package
# here is extra attack surface and extra findings for the vulnerability scan.
-r requirements.txt

aiosqlite==0.20.0        # the suite runs on SQLite instead of Postgres
pytest==8.2.2
pytest-asyncio==0.23.8   # the concurrency tests in test_load_behaviour.py
httpx==0.27.0
fakeredis==2.23.3
lupa==2.1                # gives fakeredis real Lua, so tests run the production script
locust==2.31.2           # load harness, see locustfile.py
