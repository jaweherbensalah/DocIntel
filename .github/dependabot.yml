version: 2
updates:
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5

  # Actions are third-party code in our release path, so they need the same
  # upgrade discipline as application dependencies.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
